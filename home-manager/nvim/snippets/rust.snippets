snippet display "Display trait implementation"
	impl std::fmt::Display for ${1:Type} {
		fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> Result<(), std::fmt::Error> {
			${0}
		}
	}

snippet default "Default trait implementation"
	impl Default for ${1:Type} {
		fn default() -> Self {
			${0}
		}
	}

snippet from "From trait implementation"
	impl From for ${1:Type} {
		fn from(value: T) -> Self {
			${0}
		}
	}
